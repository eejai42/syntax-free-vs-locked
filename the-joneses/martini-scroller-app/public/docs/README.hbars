<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
<style>
  table {
    font-size: normal;
  }
  h1 {
    font-size: 1.2rem;
    font-weight: bold;
  }

  h2 {
    font-size: 1.1rem;
    font-weight: bold;
  }
  .goal {
    color: #dddddd; 
    font-weight: bold; 
    width: 15rem; 
    min-height: 2rem; 
    border: solid black 1px"
    text-align: center;
  }
  .current {
    float: left; color: #ddd; 
    font-weight: bold; 
    width: 15rem; 
    min-height: 2rem; 
    border: solid black 1px
    text-align: center;
  }
  .stale {
    color: red; 
    font-weight: bold; 
    width: 90%; 
    text-align: center; background-color: #FFFFFF; 
    margin-left: auto; margin-right: auto
  }
  .match-row {
    min-height: 3rem;
  }
  .platform-image {
    margin: 0.5rem; 
    width: 6rem; 
    float: left; 
    margin-top: 0;
  }

  .from-node-image {
    padding: 0rem; 
    border-right: dashed black 3px; 
    height: 27rem;
    padding-top: 0;
  }

  .transpiler-attachments {
    margin: 0.5rem; 
    width: 5rem; 
    float: left;
  }

  .from-node-attachment {
    margin: 0.5rem; 
    width: 8rem;
  }

  .transpiler-node {
    height: 30rem;
    margin: 0rem;
    border: solid purple 2px;
  }

  .out-of-sync {
    border: solid red 10px;
  }

  .in-sync {
    border: solid green 10px;
  }

  body {
    font-size: 1rem;
  }

</style>
{{#with this}}

<table>
<tbody>
<tr><td valign="top">
<hr></hr>

<h1 style="text-align: center">Syntax-Locked Methodology</h1>
<hr></hr>

  {{#each this }}
    {{#each this }}
      <div class="transpiler-node" style="background-color: {{#if (eq IsUsed 0)}}lightgray;{{else}}{{#unless (eq (length LockedColor) 0)}}{{#if (eq LockedColor ExpectedColor)}}lightgreen{{else}}#ff9ac0;{{/if}}{{/unless}}{{/if}}">

      {{#if (eq (length LockedColor) 0)}}
        <div style="margin: 0.5rem; height: 30rem; text-align: center; horizontal-align: middle;">
    <div>Normal text</div>
          <div style="padding: 10%; background-color: #eeee00; ">-- Missing Layer --</div>
        </div>
      {{else}}
        <table style="border: none; width: 100%">
          <tbody>
            <tr>
            <td valign="top">
    <div>Normal text2</div>

              <div class="from-node-image">
                {{#if FromNodeAttachments}}
                  {{#each FromNodeAttachments}}<img src="{{this}}" alt="From Node Attachment Image" class="from-node-attachment">{{/each}}
                {{/if}}
                </div>
              </div>
            </td>
            <td valign="top">
              <table><tr>
                <td>
                  <div class="goal match-row row in-sync" style="background-color: {{ExpectedColor}};">Goal: {{ExpectedColor}}</div>
                </td>
                <td valign="top">
                  <div class="current match-row {{#if (eq LockedColor ExpectedColor)}}in-sync{{else}}out-of-sync{{/if}}" style="background-color: {{LockedColor}};"> vs. SYNTAX-LOCKED {{LockedColor}}
                    {{#unless (eq LockedColor ExpectedColor)}}
                      <div class="stale">⬆ STALE ⬆</div>
                    {{/unless}}
                  </div>
                </td>
                <tr><td colSpan="2">
                  <h2> {{#if (eq IsUsed 0)}} ****** UNUSED ******* {{/if}}{{FromNodeName}} ➡ {{EdgeName}}</h2>
                </td></tr>
                <tr><td colSpan="2">
                  {{#if TranspilerNodeAttachments}}
                    {{#each TranspilerNodeAttachments}}<img src="{{this}}" alt="Transpiler Node Attachment Image" class="transpiler-attachments">{{/each}}
                  {{/if}}
                  <h4 style="clear: left">{{TransformerNodeName}}</h4>
                </td></tr>
                <tr><td>
                  <div style="display: block;">
                  {{#if ToNodeAttachments}}
                    {{#each ToNodeAttachments}}<img src="{{this}}" alt="To Node Attachment Image" style="margin: 0.5rem; width: 8rem;">{{/each}}
                  {{/if}}
                </div>

                <div>
                  <b>{{ToNodeType}}</b> {{ToNodeName}}  ➡ {{OutputFileName}}</b>
                </div>
                
                <div style="display: block; clear: left;"></div>
                </td></tr>
              </table>
            </td>          
            <td halign="right" valign="top">
              {{#if ToNodePlatformAttachments}}
                {{#each ToNodePlatformAttachments}}<img src="{{this}}" alt="To Node Platform Attachment Image" class="platform-image">{{/each}}
              {{/if}}
            </td></tr>
          </tbody>
        </table>
        <div style="display: block; clear: left;">
      </div>
    {{/if}}
  </div>
  {{/each}}
{{/each}}


</td>
<td valign="top">


  <hr></hr>

<h1 style="text-align: center">Syntax-Free Methodology</h1>

  <hr></hr>
  {{#each this }}
    {{#each this }}
        <div class="transpiler-node"  style="background-color: {{#if (eq SyntaxFreeColor ExpectedColor)}}lightgreen;{{else}}#ff9ac0{{/if}}">

      {{#unless false}}

        <table style="border: none; width: 100%;"><tr>
        <td valign="top">
          <div class="from-node-image">
            {{#if FromNodeAttachments}}
              {{#each FromNodeAttachments}}<img src="{{this}}" alt="From Node Attachment Image" class="from-node-attachment">{{/each}}
            {{/if}}
            </div>
          </div>
        </td>
        <td valign="top">
          <table><tr>
            <td>
              <div class="goal match-row in-sync" style="background-color: {{ExpectedColor}};">Goal: {{ExpectedColor}}</div>
            </td>
            <td valign="top">
              <div class="current match-row  {{#if (eq SyntaxFreeColor ExpectedColor)}}in-sync{{else}}out-of-sync{{/if}}" style="background-color: {{SyntaxFreeColor}};"> vs. SYNTAX-FREE {{SyntaxFreeColor}}
              
              {{#unless (eq SyntaxFreeColor ExpectedColor)}}
                <div class="stale">⬆ STALE ⬆</div>
              {{/unless}}

              </div>


            </td>
            <tr><td colSpan="2">          
              <h2> {{#if (eq IsUsed 0)}} ****** UNUSED ******* {{/if}}{{FromNodeName}}  ➡ {{EdgeName}}</h2>
            </td></tr>
            <tr><td colSpan="2">          
              {{#if TranspilerNodeAttachments}}
                {{#each TranspilerNodeAttachments}}<img src="{{this}}" alt="Transpiler Node Attachment Image" class="transpiler-attachments">{{/each}}
              {{/if}}
              <h4 style="clear: left">{{TransformerNodeName}}</h4>
            </td></tr>
            <tr><td colSpan="2">
              {{#if FromEdgeFreeAttachments}}
                {{#each FromEdgeFreeAttachments}}<img src="{{this}}" alt="From Edge Free Attachment Image" style="margin: 0.5rem; width: 5rem; float: left;">{{/each}}
              {{/if}}

              {{#if FreeTranspilerAttachments}}
                {{#each FreeTranspilerAttachments}}<img src="{{this}}" alt="FreeTranspilerAttachment Image" style="margin: 0.5rem; width: 5rem;">{{/each}}
              {{/if}}

              {{#if ToNodeAttachments}}
                {{#each ToNodeAttachments}}<img src="{{this}}" alt="To Node Attachment Image" style="margin: 0.5rem; width: 8rem;">{{/each}}
              {{/if}}

              <div style="display: block; clear: left;">
                <div>
                  {{#if (eq (length SyntaxFreeEdgeOutputFileName) 0)}}{{TransformerNodeName}}{{else}}{{SyntaxFreeEdgeOutputFileName}}{{/if}} ➡ 
                  {{#unless (eq (length FreeTranspilerNodeName) 0)}}{{FreeTranspilerNodeName}} ➡ {{/unless}}
                  {{ToNodeType}} <b>{{OutputFileName}}</b>   ({{ToNodeName}})
                </div>
              </div>
            </td></tr>

          </table>
        </td>          
        <td halign="right" valign="top">
          {{#if ToNodePlatformAttachments}}
            {{#each ToNodePlatformAttachments}}<img src="{{this}}" alt="To Node Platform Attachment Image" class="platform-image">{{/each}}
          {{/if}}

        </td></tr>
        </table>
        </div>
      {{/unless}}
    {{/each}}
  {{/each}}
</td>
</tr>
</tbody>
</table>

{{/with}}
